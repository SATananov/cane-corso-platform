# Step 111.3 — Heritage Navigation & Member Home Clarity Polish

## Purpose

Make the new USG Heritage surface discoverable and make the logged-in member entry clearer without changing authority/data flows.

## Scope

- Add public navigation link to `/heritage` as `История` / `Heritage` / `Storia`.
- Rename the member workline label from `Старт` / `Start` to `Център` / `Center` / `Centro`.
- Add a subtle USG seal/identity element in the platform hero empty space.
- Add a compact next-step guidance card to the authenticated platform action panel.

## Boundaries preserved

No Registry, Certificate, Verify, Gallery, Admin authority, Auth/session, DB schema, or Health Tracker backend logic changed.
