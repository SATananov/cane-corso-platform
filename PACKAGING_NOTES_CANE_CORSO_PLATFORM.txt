Cane Corso Platform — packaging notes

This package set was prepared from the current working tree in /mnt/data/cc_work.

Included artifacts:
1) cane-corso-platform_patch_changed-files_v3_actual.zip
   - only files changed in the current working tree
   - paths are relative to repo root for copy-paste overlay

2) cane-corso-platform_clean-source_release.zip
   - clean source package from current working tree
   - excludes .git, node_modules, .next, .turbo, .expo, env files, and common caches

Verification executed in the working tree:
- node scripts/check-workshop-syntax.mjs  -> passed
- node scripts/verify-workshop-foundation.mjs -> passed

TypeScript status:
- direct web tsc invocation still reports missing type definition files for node/react/react-dom
  in this container environment, so full typecheck is NOT claimed as passed here.
