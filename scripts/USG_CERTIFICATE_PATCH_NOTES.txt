USG Certificate exact-style + admin photo selection patch

What changed:
1. Certificate layout is polished closer to the approved premium reference:
   - larger certificate canvas and stronger 3:4 proportions
   - larger Cane Corso emblem and stronger UNICO SUO GENERE header
   - bigger EN/BG/IT authenticity title
   - clearer left image/QR panel
   - larger official seal and more handwritten-style signature
   - Verify row removed from the table; QR/Verify remains in the QR + footer area.

2. Admin photo selection is clearer:
   - /review now shows a direct button on each owner-uploaded image:
     BG: "Избери за сертификат"
   - If no certificate exists yet, the button issues the USG certificate with that image.
   - If a certificate already exists, the same action updates the certificate photo.
   - The selected certificate photo is shown as selected and the button is disabled for that image.

3. Public registry admin panel:
   - Admin can also change the certificate photo from the published registry profile.
   - This uses the same certificate issue/update action and keeps Registry / Gallery / Certificate separated.

Checks run:
- node scripts/check-workshop-syntax.mjs ✅

Recommended local checks after applying:
pnpm --filter @cane-corso-platform/web typecheck
pnpm build
pnpm dev
