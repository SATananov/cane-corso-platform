Step 3 — Public Registry Detail + Admin Media Control

Included:
- Public Registry now reads only admin/registry-visible dog_media images for public gallery/hero.
- Registry profile no longer falls back to private owner mainImageUrl when admin hides images.
- Public detail pedigree tree is visible on the public profile, while deeper story/details remain member-gated.
- Admin Review queue now shows all owner-uploaded images.
- Admin can make an image primary.
- Admin can toggle Registry visibility per image.
- Admin can toggle USG Gallery inclusion per image.
- USG Gallery page now renders real admin-selected USG Gallery entries only when an active certificate exists.
- Added dog_media DB columns: visible_in_registry, visible_in_usg_gallery.

Apply:
1. Copy the patch files into the repo, preserving folders.
2. Run pnpm db:migrate.
3. Run pnpm workspace:syntax.
4. Run pnpm workspace:verify.
5. Run pnpm --filter @cane-corso-platform/web typecheck.
