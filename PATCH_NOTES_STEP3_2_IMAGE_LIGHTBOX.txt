Step 3.2 — Image Lightbox Preview

Changed files:
- apps/web/components/image-lightbox.tsx
- apps/web/components/review-queue-dashboard.tsx
- apps/web/components/public-registry-profile.tsx
- apps/web/app/globals.css

What this patch adds:
- Clicking an owner photo in the admin review queue opens it in a premium full-page overlay.
- Clicking the main public registry photo opens it in the same overlay.
- Clicking gallery thumbnails in the public registry detail opens them in the same overlay.
- Clicking parent photos in the public registry detail opens them in the same overlay.
- The overlay has dark USG styling, close button, backdrop click close, and Escape key close.

No DB migration is required for this patch.
Header, Home, and the stable /my-dogs logic are not changed.
